the 12 factor app
  by Heroku

Try to create multiple folders for both client and server
-make package.json in root directory
- for apps deployed to heroku, bower must be npm installed (bower doesnt know(or care) that its globally installed)
